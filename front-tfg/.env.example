# Copia este archivo a `.env` y ajusta las credenciales de PostgreSQL.
# Ejemplo para un servidor local con usuario postgres/postgres y base de datos tfg:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tfg?schema=public"
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/DB_NAME?schema=public"
